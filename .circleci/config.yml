version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: Install Packages
          command: npm install
      - run:
          name: Run Lint
          command: npm run lint
