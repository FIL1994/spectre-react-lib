import * as React from "react";

interface Props {
  className?: string;
  centered?: boolean;
}

/** A page for containing elements. */
declare const Page: React.FunctionComponent<Props>;

export default Page;
