import * as React from "react";

interface Props {
  className?: string;
  style?: React.CSSProperties;
  active?: boolean;
}

declare const TabHeading: React.FunctionComponent<Props>;

export default TabHeading;
