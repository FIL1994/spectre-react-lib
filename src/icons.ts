/**
 * Public boundary for optional icon components.
 *
 * The typed Icon component will be added here without growing the stable root
 * entrypoint. Keep this module side-effect free so importing it never loads CSS.
 */
export {};
